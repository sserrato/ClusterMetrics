  <div class="ui-content">
    <h1>Email classification</h1>
    <p>
      add filter for greater than equal to four
    </p>
  <p>Download:
   <%= link_to 'CSV', emails_classify_path(format "csv") %> |
   <%= link_to 'Excel', emails_classify_path(format "xls") %>
  </p>
</div>
<div class="ui-content">
  <table class="ui-responsive ui-shadow">
    <thead>
      <tr>
        <th>ID</th>
        <th>Email Domain</th>
        <th>Bridge</th>
        <th>Frequency</th>
        <th>Cluster</th>
        <th>Month</th>
        <th>Year</th>
      </tr>
      <tbody>
        <% @emails_bridge0.each do |eb| %>
            <tr>
              <td><%= eb.id %> : <%= link_to "EDIT",edit_email_path(eb.id)%> </td>
              <td><%= eb.emaildomain %></td>
              <td><%=eb.bridge%></td>
              <td><%=eb.email_frequency%></td>
              <td><%=eb.cluster_id%></td>
              <td><%=eb.month%></td>
              <td><%=eb.year%></td>
            <% end %>
            </tr>
      </tbody>
  </table>

</div>
<%= @testVar %>
<%= $testVar %>
<%= form_tag "tst" do %>
  <%= radio_button_tag "account", "default info" %>
  <%= submit_tag "Save" %>
<% end %>
